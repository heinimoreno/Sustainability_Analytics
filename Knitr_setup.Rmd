
```{r include = FALSE}
# Include this setup at the beginning of each Rmd file so you can render individual chapters and the whole document
# with consistent knittr settings.

library(knitr)
# Default settings for including or excluding code (echo) and output (results).
opts_chunk$set(message = FALSE, echo = TRUE, results = TRUE, warning = FALSE)
```